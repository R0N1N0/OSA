
import Atropos from 'atropos'

const myAtropos = Atropos({
  el: ".my-atropos",
  activeOffset: 40,
  shadowScale: 1.05,
});

const myAtropos2 = Atropos({
    el: ".my-atropos-2",
    activeOffset: 40,
    shadowScale: 1.05,
  });
